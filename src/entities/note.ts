export interface Note {
  id: string;
  title: string;
  startDate: string;
  endDate: string;
  summary: string;
  timeCreated: string;
  lastTimeUpdated: string;
  color: string; // todo: add this later as a feature
}
